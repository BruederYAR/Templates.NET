# Templates.NET 

A set of templates for .NET

You can go to the template directory and read the description in the readme file

To install a template for your device:
1. Go to the directory with the desired template
    ```bash
    cd desired_template
    ```
2. Perform the installation
   ```bash
   dotnet new --intall .
   ```